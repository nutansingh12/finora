const path = require('path');

module.exports = {
  dependencies: {
    // Removed gesture handler configuration
  },
  project: {
    ios: {},
    android: {},
  },
  assets: ['./src/assets/fonts/'],
};
